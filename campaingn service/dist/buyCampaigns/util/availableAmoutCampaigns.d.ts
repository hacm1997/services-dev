export declare class AvailableAmoutCampaigns {
    addCampaignsAmount(numberCampaignsAdd: number, numberCampaignsCurrent: number): number;
    subtractCampaignAmount(quantity: number, send: number): number;
}
