export enum TypeContact {
  contact = 'contacto',
  prospect = 'prospect',
}
export enum StatusEmailview {
  abierto = 'open',
  cerrado = 'close',
}
export enum ReceiveEmail {
  si = 'si',
  no = 'no',
}
